---
- name: Very Simple Trial Playbook
  hosts: localhost
  connection: local
  tasks:
    - name: Copy the text file to the tmp directory
      ansible.builtin.copy:
        src: "{{ playbook_dir }}/hello.txt"
        dest: "/tmp/hello-deployed.txt"
